[Unit]
Description=Monitor I/O usage of logging and middleware processes

[Service]
Type=simple
ExecStart=/bin/sh -c 'iostat -y -d -x mmcblk0p3 5 425 > /tmp/iostat_monitor.log'
Restart=no

[Install]
WantedBy=multi-user.target
